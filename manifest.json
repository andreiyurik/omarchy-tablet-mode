{
  "schemaVersion": 1,
  "id": "andreiyurik.tablet-mode",
  "name": "Tablet Mode",
  "version": "1.5.0",
  "author": "Andrei Yurik",
  "license": "MIT",
  "homepage": "https://github.com/andreiyurik/omarchy-tablet-mode",
  "description": "Makes tablet mode work on a 2-in-1: the screen follows the accelerometer, the touchscreen and pen follow the screen, the built-in keyboard goes quiet while folded and an on-screen keyboard plugin comes up. The bar button appears once folded, to lock the rotation",
  "kinds": [
    "service",
    "bar-widget"
  ],
  "entryPoints": {
    "service": "Service.qml",
    "barWidget": "RotationLock.qml"
  },
  "barWidget": {
    "displayName": "Tablet Mode",
    "description": "Shows up once the machine is folded. Tap it to lock the rotation, turn the screen by hand, or show and hide the on-screen keyboard. Middle-click turns the screen.",
    "category": "Hardware",
    "defaultSection": "right",
    "allowMultiple": false,
    "defaults": {
      "mapping": "auto",
      "allPositions": false,
      "keyboardOnFold": true
    },
    "schema": [
      {
        "key": "mapping",
        "type": "enum",
        "label": "Which way the screen turns",
        "options": [
          "auto",
          "standard",
          "portrait-swapped",
          "landscape-swapped",
          "rotated-180"
        ],
        "defaultValue": "auto",
        "description": "How the accelerometer is mounted relative to the panel, the one thing here that cannot be probed: the sensor reports which way is up, but not how it was screwed in. Auto uses the mounting already established for your model, and standard for any model not yet known. If turning the machine rotates the screen the wrong way, pick the entry naming the pair that is wrong -- portrait-swapped when only the two upright positions are mirrored, landscape-swapped when only the two flat ones are, rotated-180 when everything is upside down -- and please file a machine report so auto gets it right for the next person."
      },
      {
        "key": "allPositions",
        "type": "boolean",
        "label": "Rotate in laptop mode too",
        "defaultValue": false,
        "description": "Off means the accelerometer is followed only once the lid is folded back past the keyboard, which is how a tablet behaves and why a laptop on your knees does not flip. On follows it always, which is the setting for a machine with no fold sensor -- there the daemon rotates in every position regardless."
      },
      {
        "key": "keyboardOnFold",
        "type": "boolean",
        "label": "Show the on-screen keyboard when folded",
        "defaultValue": true,
        "description": "With an on-screen keyboard plugin enabled (On-Screen Keyboard or Omaqwerty from plugins.omarchy.org), folding the machine brings it up and opening it puts it away again. A keyboard opened by hand is left alone."
      }
    ]
  }
}
