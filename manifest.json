{
  "schemaVersion": 1,
  "id": "andreiyurik.tablet-mode",
  "name": "Tablet Mode",
  "version": "1.4.0",
  "author": "Andrei Yurik",
  "license": "MIT",
  "homepage": "https://github.com/andreiyurik/omarchy-tablet-mode",
  "description": "Makes tablet mode work on a 2-in-1: the screen follows the accelerometer, the touchscreen and pen follow the screen, the built-in keyboard goes quiet while folded, and a touch-friendly bar panel holds the rotation lock and pen settings",
  "kinds": [
    "service",
    "bar-widget"
  ],
  "entryPoints": {
    "service": "Service.qml",
    "barWidget": "RotationLock.qml"
  },
  "barWidget": {
    "displayName": "Tablet Mode",
    "description": "Tap for the rotation lock, turning the screen by hand, and pen settings. Middle-click turns the screen.",
    "category": "Hardware",
    "defaultSection": "right",
    "allowMultiple": false,
    "defaults": {
      "mapping": "auto",
      "allPositions": false,
      "hideInLaptopMode": false,
      "hideCursorWithPen": false,
      "penPressure": "normal"
    },
    "schema": [
      {
        "key": "mapping",
        "type": "enum",
        "label": "Which way the screen turns",
        "options": [
          "auto",
          "standard",
          "portrait-swapped",
          "landscape-swapped",
          "rotated-180"
        ],
        "defaultValue": "auto",
        "description": "How the accelerometer is mounted relative to the panel, the one thing here that cannot be probed: the sensor reports which way is up, but not how it was screwed in. Auto uses the mounting already established for your model, and standard for any model not yet known. If turning the machine rotates the screen the wrong way, pick the entry naming the pair that is wrong -- portrait-swapped when only the two upright positions are mirrored, landscape-swapped when only the two flat ones are, rotated-180 when everything is upside down -- and please file a machine report so auto gets it right for the next person."
      },
      {
        "key": "allPositions",
        "type": "boolean",
        "label": "Rotate in laptop mode too",
        "defaultValue": false,
        "description": "Off means the accelerometer is followed only once the lid is folded back past the keyboard, which is how a tablet behaves and why a laptop on your knees does not flip. On follows it always, which is the setting for a machine with no fold sensor -- there the daemon rotates in every position regardless."
      },
      {
        "key": "hideInLaptopMode",
        "type": "boolean",
        "label": "Hide the icon in laptop mode",
        "defaultValue": false,
        "description": "The button has little to do while the machine is open and the screen is not rotating, so this keeps it out of the bar until it does. It reappears the moment the machine is folded, or while a lock is being held."
      },
      {
        "key": "hideCursorWithPen",
        "type": "boolean",
        "label": "Hide the cursor while writing",
        "defaultValue": false,
        "description": "Hides the pointer while the pen is near the screen, until a mouse or touchpad moves. Applies to every pen, which on a laptop is the one it has."
      },
      {
        "key": "penPressure",
        "type": "enum",
        "label": "Pen pressure",
        "options": [
          "soft",
          "normal",
          "firm"
        ],
        "defaultValue": "normal",
        "description": "Soft reaches full pressure well before pressing hard; firm ignores the lightest touch. Normal leaves the pen's own range, and whatever input.lua sets, alone."
      }
    ]
  }
}
